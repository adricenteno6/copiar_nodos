---
- name: Sync Pull task - Executed on "{{groups['app']["i"]}}"
  hosts: "{{groups['app'][0]}}"
  tasks:   
    - name: Copy the file from Ubuntu to CentOs using Method Pull
      tags: sync-pull
      synchronize:
        src: "{{ item }}"
        dest: "{{ item }}"
        mode: pull
      delegate_to: "{{groups['app'][1]}}"
      register: syncfile
      run_once: true
